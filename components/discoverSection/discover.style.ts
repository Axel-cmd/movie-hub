import { StyleSheet } from "react-native";
import theme from "../../theme/theme";

export default StyleSheet.create({
    container: {
        marginBottom: 10,
        marginTop: 10
    },
    title: {
        color: theme.text,
        fontSize: 24,
        fontWeight: "bold",
        width: "100%"
    }
})