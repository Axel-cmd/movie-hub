import { StyleSheet } from "react-native";

export default StyleSheet.create({
    card: {
        borderWidth: 1,
        width: 120,
        justifyContent: 'center',
        marginRight: 2,
        borderRadius: 5
    },
    img: {
        width: "100%",
        height: "100%",
        resizeMode: "cover",
        borderRadius: 5
    }
})