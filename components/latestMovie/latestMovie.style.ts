import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        width: "100%",
        height: "100%",
        // backgroundColor: "#fff"

    },
    image: {
        width: "100%", 
        height: 400,
        resizeMode: "cover",
        marginBottom: 10,
    }
})